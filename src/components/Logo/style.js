import styled from "styled-components";

export const StyledLogo = styled.div`
  width: fit-content;
  height: fit-content;

  font-size: 20pt;
  font-weight: 800;
  line-height: 150%;

  color: var(--color-primary);
`;
